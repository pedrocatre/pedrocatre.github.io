#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../shelljs/bin/shjs" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../shelljs/bin/shjs" "$@"
  ret=$?
fi
exit $ret
