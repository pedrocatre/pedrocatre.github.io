function numLines(e){return typeof e=="string"?e.match(/\n/g).length:0}var shell=require(".."),assert=require("assert"),path=require("path"),fs=require("fs");fs.existsSync=fs.existsSync||path.existsSync,assert.equal(shell.silent(),!1),shell.silent(!0),assert.equal(shell.silent(),!0),shell.silent(!1),assert.equal(shell.silent(),!1),shell.exit(123);