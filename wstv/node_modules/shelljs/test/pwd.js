function numLines(e){return typeof e=="string"?e.match(/\n/g).length:0}var shell=require(".."),assert=require("assert"),path=require("path");shell.silent(!0),shell.rm("-rf","tmp"),shell.mkdir("tmp");var _pwd=shell.pwd();assert.equal(shell.error(),null),assert.equal(_pwd,path.resolve(".")),shell.cd("tmp");var _pwd=shell.pwd();assert.equal(shell.error(),null),assert.equal(path.basename(_pwd),"tmp"),shell.exit(123);