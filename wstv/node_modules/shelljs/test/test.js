var shell=require(".."),assert=require("assert"),path=require("path"),fs=require("fs");fs.existsSync=fs.existsSync||path.existsSync,shell.silent(!0),shell.rm("-rf","tmp"),shell.mkdir("tmp");var result=shell.test();assert.ok(shell.error());var result=shell.test("asdf");assert.ok(shell.error());var result=shell.test("f","resources/file1");assert.ok(shell.error());var result=shell.test("-f");assert.ok(shell.error());var result=shell.test("-f","resources/file1");assert.equal(shell.error(),null),assert.equal(result,!0);var result=shell.test("-d","resources/file1");assert.equal(shell.error(),null),assert.equal(result,!1);var result=shell.test("-f","resources");assert.equal(shell.error(),null),assert.equal(result,!1);var result=shell.test("-d","resources");assert.equal(shell.error(),null),assert.equal(result,!0),shell.exit(123);